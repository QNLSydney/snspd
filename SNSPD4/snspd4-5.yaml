

att_blue_name: 'V1550PA'
  # device_name should be old device name 
  # v_attenuator should be 4.7 for ~100dB inline


    # Thresholds from ID 20
    # if current >= 2e-6:
    #     threshold1 = -24e-3 # in volts
    #     threshold2 = -10e-3 # in volt


    # elif current >= 1.5e-6:
    #     threshold1 = -11.2e-3 # in volts
    #     threshold2 = -8.5e-3 # in volt


    # elif current >= 1e-6: 
    #     threshold1 = -9e-3 
    #     threshold2 = -8e-3
        
    # else:
    #     threshold1 = -6.5e-3 # in volts
    #     threshold2 = -4.5e-3 # in volt

  # 
  # # Parameters from SNSPD4_5_Counts_vs_Current.ipynb
  # v_attenuator = 4.7
  # v_scale_old_device = 50e-3
  # current_range = np.arange(1e-7, 4.1e-6, 1e-7)
  # device_name = 'Line 2 Old Device'

    